@using CarTracker.WebMvcApp.Entities
@using CarTracker.WebMvcApp.ViewModels
@model IEnumerable<CarTracker.WebMvcApp.Entities.Logs.RepairLog>


<style>
    td {
        border: 1px solid black;
        width: 150px;
    }

    thead td {
        background-color: aquamarine;
    } 
</style>

<h1>RepairLog</h1>

<select>
    @foreach (var cars in Model) {
        <option>@cars.Nickname</option>

    }
</select>

@* <table style="border: 1px solid black; border-collapse: collapse ">
    <thead>
        <tr>
            <td>Id</td>
            <td>Model</td>
            <td>Nickname</td>
            <td>Color</td>
            <td>Brand</td>
            <td>Bought Time</td>
            <td>Fuel Type</td>
            <td>Owner</td>
        </tr>
    </thead>
    <tbody>

        @foreach (var users in Model)
        {
            <tr>
                <td>@users.Id</td>
                <td>@users.Model</td>
                <td>@users.Nickname</td>
                <td>@users.Color</td>
                <td>@users.Brand</td>
                <td>@users.BoughtTime</td>
                <td>@users.FuelType</td>
                <td>@users.Owner</td>
            </tr>	
        }
    </tbody>
</table> *@